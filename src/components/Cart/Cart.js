import React, { useState } from 'react';

const Cart = (props) => {
    
    

    return (
        <div>
            <h3>This is Cart: {props.count}</h3>
        </div>
    );
};

export default Cart;